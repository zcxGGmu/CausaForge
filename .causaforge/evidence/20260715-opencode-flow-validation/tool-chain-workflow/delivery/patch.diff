diff --git a/src/migrate.ts b/src/migrate.ts
index 2c8d109..51bbeac 100644
--- a/src/migrate.ts
+++ b/src/migrate.ts
@@ -1,3 +1,3 @@
 export function migrate(input) {
-  return { id: input.id }
+  return { id: input.id, requiredField: input.requiredField }
 }
